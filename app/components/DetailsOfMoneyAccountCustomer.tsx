"use  client";
import { memo } from "react";

const DetailsOfMoneyAccountCustomer = ({ id }: { id: number}) => {
  return <div>
    {id}
  </div>;
};
export default memo(DetailsOfMoneyAccountCustomer);
